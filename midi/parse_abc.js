function parseABC(str) {
    const lines = str.split('\n');

    const parser = {
        parse: function(str) {

        }
    };

    for (const line of lines) {
        const tokens = line.strip().split(' ');
        
    }
}